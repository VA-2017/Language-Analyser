#pragma once
#include <iostream>
#include <vector>
#include <string>
#include <fstream>


class TextFileReader {
public:
    std::vector<std::string> ReadAllTextFiles(std::vector<std::string> FilePaths);
private:

};